#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void divisor(){
	for(int i=1000000; i<1; i=i/10);}

int main(){
	int num = 0;
	printf("Introduce un número: ");
	scanf("%d", &num);
	
	while(num>0){
	void divisor;	
	num=num/divisor;
	printf("La división entre %d y %d es %d \n", num, divisor, num);
	}

return 0;
}
